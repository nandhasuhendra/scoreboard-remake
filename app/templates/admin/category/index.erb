<% tables = Terminal::Table.new do |table| %>
  <% table.headings = ['Id', 'Category', 'Created at', 'Updated at'] %>
  <% @resources.each do |category| %>
    <% table.add_row [category.id, category.category, category.created_at, category.updated_at] %>
  <% end %>
<% end %>

<%= tables %>
