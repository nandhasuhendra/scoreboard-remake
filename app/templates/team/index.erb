<% tables = Terminal::Table.new do |table| %>
  <% table.headings = ['Id', 'Username', 'Password'] %>
  <% @user.each do |user| %>
    <% table.add_row [user.id, user.username, user.password_digest] %>
  <% end %>
<% end %>

<%= tables %>
